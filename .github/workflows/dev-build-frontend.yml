name: Dev Frontend Build

on:
  workflow_dispatch:
  push:
    branches:
      - dev
    paths:
      - 'frontend/**'

concurrency:
  group: dev-pipeline
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/kiwi
  IMAGE_TAG: dev

jobs:
  build-images:
    name: Build and push Frontend Dev
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Lowercase Image Prefix
        run: |
          echo "IMAGE_PREFIX=${IMAGE_PREFIX,,}" >> ${GITHUB_ENV}
        env:
          IMAGE_PREFIX: ${{ env.IMAGE_PREFIX }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v5
        with:
          path: ${{ runner.temp }}/.buildx-cache-frontend-builder
          key: docker-${{ runner.os }}-frontend-builder-${{ github.sha }}
          restore-keys: |
            docker-${{ runner.os }}-frontend-builder-

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.IMAGE_PREFIX }}/frontend:${{ env.IMAGE_TAG }}
          target: builder
          build-args: NEXT_PUBLIC_API_URL=/api
          cache-from: type=local,src=${{ runner.temp }}/.buildx-cache-frontend-builder
          cache-to: type=local,dest=${{ runner.temp }}/.buildx-cache-frontend-builder-new,mode=max

      - name: Move cache
        run: |
          rm -rf ${{ runner.temp }}/.buildx-cache-frontend-builder
          mv ${{ runner.temp }}/.buildx-cache-frontend-builder-new ${{ runner.temp }}/.buildx-cache-frontend-builder

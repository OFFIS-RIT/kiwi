diff --git a/dist/core/workflow-function.d.ts b/dist/core/workflow-function.d.ts
index efd435287f73953e8549b83f8aaece0dde859aa9..546b2b707a1fdbb92ca524fef265b831363472c8 100644
--- a/dist/core/workflow-function.d.ts
+++ b/dist/core/workflow-function.d.ts
@@ -68,7 +68,18 @@ export interface StepApi {
 /**
  * Read-only workflow run metadata exposed to workflow functions.
  */
-export type WorkflowRunMetadata = Pick<WorkflowRun, "id" | "workflowName" | "createdAt" | "startedAt">;
+/**
+ * Error metadata exposed when a workflow step fails and OpenWorkflow is about
+ * to decide whether the step should retry or terminally fail.
+ */
+export interface WorkflowRetryState {
+    readonly retryAttempt: number;
+    readonly retryMaxAttempts: number;
+    readonly retryTerminal: boolean;
+}
+export type WorkflowRunMetadata = Pick<WorkflowRun, "id" | "workflowName" | "createdAt" | "startedAt"> &
+    WorkflowRetryState;
+export interface WorkflowRetryError extends Error, WorkflowRetryState {}
 /**
  * Params passed to a workflow function for the user to use when defining
  * steps.
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 2d59da6f5a0889f97858ba401a0a40605e6847c0..3fb99e821eb27b6e4956e797ad3d7769144e31fa 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -1,7 +1,7 @@
 export type { OpenWorkflowOptions } from "./client/client.js";
 export { OpenWorkflow } from "./client/client.js";
 export type { RetryPolicy, Workflow } from "./core/workflow-definition.js";
-export type { WorkflowRunMetadata } from "./core/workflow-function.js";
+export type { WorkflowRetryError, WorkflowRetryState, WorkflowRunMetadata } from "./core/workflow-function.js";
 export { defineWorkflowSpec, defineWorkflow, declareWorkflow, } from "./core/workflow-definition.js";
 export type { WorkerOptions } from "./worker/worker.js";
 export { Worker } from "./worker/worker.js";
diff --git a/dist/worker/execution.d.ts b/dist/worker/execution.d.ts
index 05a2acdd4a43cd2ac389fc5a9eb0e7313e0543dd..b58fbbd4719297a65f8fc431eebff17265ef559e 100644
--- a/dist/worker/execution.d.ts
+++ b/dist/worker/execution.d.ts
@@ -5,7 +5,11 @@ import type { WorkflowFunction } from "../core/workflow-function.js";
 import { type WorkflowRun } from "../core/workflow-run.js";
 interface ExecutionFenceController {
     deactivate(): void;
+    parkOnSleep(signal: Error): void;
+    readonly sleepSignal: Error | null;
     isActive(): boolean;
+    isParked(): boolean;
+    canPersistStepOutcome(): boolean;
     assertActive(): void;
 }
 /** Maximum number of step attempts allowed for a single workflow run. */
diff --git a/dist/worker/execution.js b/dist/worker/execution.js
index 32ce29913c66439e0ddd3808bc3b3afbdfb8c0e5..aafa36aa7c547aade27eaae47ddbe0133826c16a 100644
--- a/dist/worker/execution.js
+++ b/dist/worker/execution.js
@@ -29,15 +29,44 @@ class StaleExecutionBranchError extends Error {
  * writing new step attempts after execution is parked/finalized.
  */
 class ExecutionFence {
+    // active: no wait signal raised yet, run owned, everything allowed.
+    // parked: a wait signal was raised but the run is still owned by this
+    //   worker; in-flight parallel branches may still persist their outcome,
+    //   but no new step attempts may be created.
+    // deactivated (neither active nor parked): the run has been released or
+    //   finalized; no writes may happen from this execution pass.
     active = true;
+    parked = false;
+    sleepSignal = null;
     deactivate() {
         this.active = false;
+        this.parked = false;
+    }
+    parkOnSleep(signal) {
+        if (!this.active && !this.parked) {
+            return;
+        }
+        const current = this.sleepSignal;
+        if (!current || signal.resumeAt.getTime() < current.resumeAt.getTime()) {
+            this.sleepSignal = signal;
+        }
+        this.active = false;
+        this.parked = true;
     }
     isActive() {
         return this.active;
     }
+    isParked() {
+        return this.parked;
+    }
+    canPersistStepOutcome() {
+        return this.active || this.parked;
+    }
     assertActive() {
         if (!this.active) {
+            if (this.parked && this.sleepSignal) {
+                throw this.sleepSignal;
+            }
             throw new StaleExecutionBranchError();
         }
     }
@@ -49,6 +78,9 @@ class StepError extends Error {
     stepName;
     stepFailedAttempts;
     retryPolicy;
+    retryAttempt;
+    retryMaxAttempts;
+    retryTerminal;
     originalError;
     constructor(options) {
         const serialized = serializeError(options.error);
@@ -57,6 +89,10 @@ class StepError extends Error {
         this.stepName = options.stepName;
         this.stepFailedAttempts = options.stepFailedAttempts;
         this.retryPolicy = options.retryPolicy;
+        this.retryAttempt = options.stepFailedAttempts;
+        this.retryMaxAttempts = options.retryPolicy.maximumAttempts;
+        this.retryTerminal =
+            options.retryPolicy.maximumAttempts > 0 && options.stepFailedAttempts >= options.retryPolicy.maximumAttempts;
         this.originalError = options.error;
     }
 }
@@ -488,7 +524,9 @@ class StepExecutor {
         // throw sleep signal to trigger postponement
         // we do not mark the step as completed here; it will be updated
         // when the workflow resumes
-        throw new SleepSignal(this.resolveEarliestRunningWaitResumeAt(resumeAt));
+        const signal = new SleepSignal(this.resolveEarliestRunningWaitResumeAt(resumeAt));
+        this.executionFence.parkOnSleep(signal);
+        throw signal;
     }
     // ---- step.runWorkflow -----------------------------------------------
     async runWorkflow(spec, input, options) {
@@ -602,7 +640,9 @@ class StepExecutor {
         const resumeAt = timeoutAt && Number.isFinite(timeoutAt.getTime())
             ? timeoutAt
             : defaultWaitTimeoutAt(workflowAttempt.createdAt);
-        throw new SleepSignal(this.resolveEarliestRunningWaitResumeAt(resumeAt));
+        const signal = new SleepSignal(this.resolveEarliestRunningWaitResumeAt(resumeAt));
+        this.executionFence.parkOnSleep(signal);
+        throw signal;
     }
     /**
      * Create (or dedupe) the child workflow run and persist the linkage on the
@@ -651,7 +691,10 @@ class StepExecutor {
      * @param retryPolicy - Retry policy for this failure
      */
     async failStepWithError(stepName, stepAttemptId, error, retryPolicy) {
-        if (!this.executionFence.isActive()) {
+        // While parked (a sibling raised a wait signal) the run is still owned
+        // by this worker, so the failure must be persisted; otherwise the
+        // attempt would be stranded in 'running' and never retried.
+        if (!this.executionFence.canPersistStepOutcome()) {
             throw new StaleExecutionBranchError();
         }
         this.runningByStepName.delete(stepName);
@@ -665,7 +708,7 @@ class StepExecutor {
             });
         }
         catch (stepFailError) {
-            if (!this.executionFence.isActive()) {
+            if (!this.executionFence.canPersistStepOutcome()) {
                 throw new StaleExecutionBranchError();
             }
             throw stepFailError;
@@ -684,7 +727,10 @@ class StepExecutor {
         if (error instanceof StaleExecutionBranchError) {
             throw error;
         }
-        if (!this.executionFence.isActive()) {
+        if (error instanceof SleepSignal) {
+            throw error;
+        }
+        if (!this.executionFence.canPersistStepOutcome()) {
             throw new StaleExecutionBranchError();
         }
         return await this.failStepWithError(stepName, stepAttemptId, error, TERMINAL_STEP_RETRY_POLICY);
@@ -790,7 +836,9 @@ class StepExecutor {
         if (Date.now() >= timeoutAt.getTime()) {
             return await this.completeSignalWaitStep(attempt, null);
         }
-        throw new SleepSignal(this.resolveEarliestRunningWaitResumeAt(timeoutAt));
+        const signal = new SleepSignal(this.resolveEarliestRunningWaitResumeAt(timeoutAt));
+        this.executionFence.parkOnSleep(signal);
+        throw signal;
     }
     /**
      * Complete a signal-wait step attempt and update internal maps.
@@ -885,6 +933,10 @@ export async function executeWorkflow(params) {
             workflowName: workflowRun.workflowName,
             createdAt: workflowRun.createdAt,
             startedAt: workflowRun.startedAt,
+            retryAttempt: workflowRun.attempts,
+            retryMaxAttempts: params.retryPolicy.maximumAttempts,
+            retryTerminal:
+                params.retryPolicy.maximumAttempts > 0 && workflowRun.attempts >= params.retryPolicy.maximumAttempts,
         });
         // execute workflow
         const output = await workflowFn({
@@ -893,6 +945,9 @@ export async function executeWorkflow(params) {
             version: workflowVersion,
             run,
         });
+        if (executionFence.sleepSignal) {
+            throw executionFence.sleepSignal;
+        }
         // mark success
         executionFence.deactivate();
         await executeWorkflowRunTransition({
diff --git a/dist/worker/worker.js b/dist/worker/worker.js
index 8f95a1635cd0cfd938d53e2171addaaaab8eefab..e9ea1773ea8a982de41c3a9dfaf5eb52f3c6a1f9 100644
--- a/dist/worker/worker.js
+++ b/dist/worker/worker.js
@@ -198,6 +198,7 @@ class WorkflowExecution {
     workflowRun;
     workerId;
     heartbeatTimer = null;
+    stopped = false;
     constructor(options) {
         this.backend = options.backend;
         this.workflowRun = options.workflowRun;
@@ -208,9 +209,13 @@ class WorkflowExecution {
      * duration.
      */
     startHeartbeat() {
+        this.stopped = false;
         const leaseDurationMs = DEFAULT_LEASE_DURATION_MS;
         const heartbeatIntervalMs = leaseDurationMs / 2;
         this.heartbeatTimer = setInterval(() => {
+            if (this.stopped) {
+                return;
+            }
             this.backend
                 .extendWorkflowRunLease({
                 workflowRunId: this.workflowRun.id,
@@ -218,6 +223,10 @@ class WorkflowExecution {
                 leaseDurationMs,
             })
                 .catch((error) => {
+                if (this.stopped || isLeaseExtensionLost(error)) {
+                    this.stopHeartbeat();
+                    return;
+                }
                 console.error("Heartbeat failed:", error);
             });
         }, heartbeatIntervalMs);
@@ -226,12 +235,16 @@ class WorkflowExecution {
      * Stop the heartbeat loop.
      */
     stopHeartbeat() {
+        this.stopped = true;
         if (this.heartbeatTimer) {
             clearInterval(this.heartbeatTimer);
             this.heartbeatTimer = null;
         }
     }
 }
+function isLeaseExtensionLost(error) {
+    return error instanceof Error && error.message === "Failed to extend lease for workflow run";
+}
 /**
  * Sleep for a given duration.
  * @param ms - Milliseconds to sleep
